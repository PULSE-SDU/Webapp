export enum FilterType {
  SELECT = 'select',
  SEARCH = 'search',
}

export enum BatteryStatus {
  NORMAL = 'Normal',
  CRITICAL = 'Critical',
  WARNING = 'Warning',
  FULL = 'Full',
}
export enum EquipmentStatus {
  Normal = 'Normal',
  Warning = 'Warning',
  Critical = 'Critical',
  Charging = 'Charging',
}

